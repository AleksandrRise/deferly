Correct all the problems regarding contrast colors, weird ui/ux actions and images, fix functionality of many features, and make this work perfectly. Description of the project: Project: Deferly — “smart snooze” for email.
Scope: Exactly 2 pages.

Landing (/)

Show recent emails (mock-first; Composio→Gmail if keys exist).

Top-right sort toggle: Importance (AI/heuristic rank) or Recency.

Click an email → right drawer with:

1–2 line summary.

Two actions:

Plan defer now: scan calendar, evaluate movability of overlapping events (flexible vs. client/interview/etc.), propose best slot (+ alternatives), generate a polite defer message, and allow Create meeting (+ apply moves).

Ask AI: mini chat to negotiate time; on Approve, create event/moves.

Calendar (/calendar)

Day view 09:00–18:00 (15-min grid), shows base events + reply blocks + moved events (badge).

Heuristics (engine):
urgency = 0.4importance + 0.4deadlineScore + 0.2*complexity; deadlineScore = max(0, 1 - hoursUntil(deadline)/48); prefer working hours; lunch low-fit unless urgency ≥ 0.9; rules: reply_now if urgent & short gap next hour; else snooze_today; else snooze_tomorrow 09:30; 30m+ → schedule_block.

APIs:
GET /api/emails, GET /api/calendar (merge in-memory replyBlocks/movedEvents), POST /api/plan (slot + movability + defer message), POST /api/suggest (simple heuristic, optional), POST /api/draft (LLM optional), POST /api/agent (chat optional).

Stack/UX: Next.js 14 (App Router), Tailwind (+forms), dayjs, framer-motion, lucide-react; mock-first with optional Composio; no plain HTML—use styled components/cards/chips.

Acceptance: Landing lists emails with Importance/Recency; drawer shows summary + best time + reason + confidence + defer message; create meeting/moves reflect on /calendar; responsive, no console errors.